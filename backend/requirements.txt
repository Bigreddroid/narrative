# Core
fastapi==0.115.14
# starlette pinned explicitly to hold the security floor (>=0.40 fixes the
# multipart DoS, CVE-2024-47874); fastapi pulls it transitively otherwise.
starlette==0.46.2
uvicorn[standard]==0.32.1
gunicorn==22.0.0
python-dotenv==1.0.1
pydantic==2.13.4
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
pgvector==0.2.5

# Redis / Job queues
redis==5.0.4
rq==1.16.2

# Rate limiting (DoS / abuse protection)
slowapi==0.1.10
limits==5.8.0

# HTTP / Scraping
httpx==0.27.0
feedparser==6.0.11
beautifulsoup4==4.12.3
lxml==5.2.1
playwright==1.44.0

# AI
# fastembed: local, free embeddings (ONNX, no torch). Default provider —
# BAAI/bge-large-en-v1.5 emits 1024-dim vectors, matching the pgvector schema.
fastembed==0.3.6
# voyageai: OPTIONAL paid embeddings provider (opt-in via paid_apis_enabled).
# The LLM is local-only (Ollama) — no paid LLM SDK is used.
voyageai==0.2.3

# Auth

# Payments
stripe==9.9.0

# Notifications
firebase-admin==6.5.0

# Storage
boto3==1.34.100

# Monitoring
sentry-sdk[fastapi]==2.3.1
posthog==3.5.0

# Utilities
tenacity==8.3.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.32  # >=0.0.18 fixes ReDoS, CVE-2024-53981
jinja2==3.1.4
orjson==3.10.3
structlog==24.1.0
